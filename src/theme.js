import { createTheme } from "@mui/material";

export const muiTheme = createTheme({
  palette: {
    secondary: {
      main: "#E33E7F",
    },
    primary: {
      main: "#ffffff",
    },
  },
});

export const styledTheme = {
  primary: "#f6304f",
  secondary: "#edede6",
  white: "#ffffff",
};
